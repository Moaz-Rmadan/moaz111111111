import React from 'react';
import { GlassCalculation } from './types';

export const GlassCalculator = ({ onAdd }: { onAdd: (calc: GlassCalculation) => void }) => {
  return <div className="p-4 bg-white rounded-xl">Glass Calculator UI</div>;
};
