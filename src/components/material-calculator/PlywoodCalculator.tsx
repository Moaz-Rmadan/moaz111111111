import React from 'react';
import { PlywoodCalculation } from './types';

export const PlywoodCalculator = ({ onAdd }: { onAdd: (calc: PlywoodCalculation) => void }) => {
  return <div className="p-4 bg-white rounded-xl">Plywood Calculator UI</div>;
};
